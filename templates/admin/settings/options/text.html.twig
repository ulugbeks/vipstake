<div class="ui__form-row">
    <label for="{{ option.slug }}">{{ option.label }}</label>
    <input type="text" id="{{ option.slug }}" name="options[{{ option.id }}]" value="{{ option.value }}">
</div>